# 第 0 步 · 事实住在哪（真源图）

> **本该最先做，我跳过了。** TRIAL_STATUS 首版把这步标成「完成」，写着「见下真源图」，
> 而下面没有真源图——那是假的。这一轮撞的坑基本都是这一步该拦下的。
>
> 方法原文：「把事实的流转链画出来，每一跳列出谁持有哪些事实。
> **凡是同一事实在两处出现，当场记为「待加断言门」。不要等它出事。**」
>
> 首册漏了这一步，六处漂移一起爆发，而当时二十项审计、九步流程、三道门全部通过。
> 本轮漏了这一步，下面这 9 处里有 5 处已经真的出事了。

---

## 流转链

```
标准/上游约定 → 私有规范 → 参数表 → 模板表(schema) → 引擎 → 蓝图 → 编译器 → 成品
```

---

## 逐条事实：谁持有，有没有第二处

### ① 编译器样式词表　**已出事**

| | |
|---|---|
| 真源 | `build_semantic_handout_from_blueprint.py` 的 `BLOCK_STYLE_IDS`(21) / `RUN_STYLE_IDS`(11) |
| 第二处 | 化学 mapping 的 `target.blockStyles/runStyles`，自称「取自编译器」 |
| 实测漂移 | **5 处**：缺 heading4、heading5、list_continuation；多出 page_break、table |
| 处置 | 本轮物理表不复述词表，直接以编译器为准。复述一份就是再造一处漂移源。 |
| 待加的门 | 比对 mapping.target 与编译器常量，不一致即报 |

### ② 共享私有规范住在册级文件里　**结构性错位**

| | |
|---|---|
| 应在 | 一份独立的私有规范文件（跨册共享） |
| 实际在 | `carve-rules/g08-chemistry-huke-2026-topic3-4/mapping/private-spec-mapping.v1.json` —— 化学**这一册**的文件里 |
| 后果 | 物理要用它，只能投影引用一个册级文件；化学册一改，物理跟着变，且没有任何东西会响 |
| 处置 | 本轮用 merge_mapping.py 投影合并、带两侧 sha256，不复制。租约释放后应上提。 |

### ③ 页眉右区文字　**已出事**

| | |
|---|---|
| 应是 | 该内容所在的目录标题 |
| 真源候选 A | 编译器第 3041 行硬编码默认值 `"八年级化学"` |
| 真源候选 B | `blueprint["headerRight"]` |
| 实际发生 | 蓝图没给 B，落到 A —— **物理讲义每个正文页眉印着「八年级化学」** |
| 待加的门 | `GATE_HEADER_NOT_GRADE_SUBJECT`：成品页眉不得出现「X年级X科目」形态 |

### ④ 角色 id：既是统计键，又是分派字面量　**已出事**

| | |
|---|---|
| 职责一 | 命中统计要能分辨两条规则 → 想让它们不同名 |
| 职责二 | 引擎 `if role == "选项行"` 按字面量分派 → 要求它们同名 |
| 实际发生 | 我为了职责一改名，断了职责二，3 处纯图片选项行共 10 张图丢归属 |
| 教训 | 一个标识符承担两个职责，就是「同一事实两处」的变体。改 id 前先 grep 谁按字面比较它。 |

### ⑤ 字符级语义（runs）　**已出事，最重**

| | |
|---|---|
| 真源应是 | 物理源自己的字符属性分布 |
| 实际 | 我标 `pending-measurement`，只继承化学的通用条目 |
| 实测差距 | 源 A10–A14：斜体 **286**、颜色 `114599` **98**；成品：**斜体 0、颜色 0** |
| 反向错误 | 继承来的化学条目「显式高亮」「底纹」在物理 **0 命中** —— 判据恒假 |
| 我的错判 | 我在表里写「未登记的 run 会让编译停下，这正是要的」。**编译没停。** 未登记的字符属性静默落为无装饰。我依赖了一道并不存在的门。 |

### ⑥ 图片身份：三套标识

| | |
|---|---|
| 标识一 | media 文件名 = 内容哈希（同一张图多处复用 → 一个文件） |
| 标识二 | `objectId` = 源 locator 派生（同一张图多处复用 → 多个 id） |
| 标识三 | 替换表的键 = 内容哈希（含 `.render` 后缀剥离规则） |
| 已踩 | 断言拿「出现次数 26」比「登记对象 19」，报假失败，还把真漏网 #159 藏在假警报后 |
| 规则 | 按对象判归属，按出现判替换次数，两者不可互比 |

### ⑦ 章/讲归属

| | |
|---|---|
| 候选 | registry.theme ／ 源图注编号 图4-x-y ／ 目录树 ／ 讲标题 |
| 本轮取 | 图注编号（A10→4-1 … A14→4-5，一一对应）→ 第四章 光。**量出来的，不是起的。** |

### ⑧ 答案

| | |
|---|---|
| 应在 | `atoms[].answer`（数据层），成品不出 |
| 实际还有 | 源正文里残留一处「【答案】D」 |
| 谁抓住 | 合规审计 `word.content.student-safety-markers` |
| 老流程 | 有剔除步骤，正式库母版是 0；**这一局老流程赢我** |

### ⑨ 媒体文件是派生物，会被重建覆盖　**已出事**

| | |
|---|---|
| 事实 | `work/media/` 是抽取产物，`--media` 参数会重新抽取并覆盖 |
| 已踩 | JPEG 元数据修复做完 → 重建蓝图 → 修复被冲掉 → 同一个除零错回来 |
| 顺序约束 | **任何对媒体的修复，必须排在最后一次媒体抽取之后** |

---

## 小结

9 条里 **5 条已经真的出事**（①③④⑤⑨），另有 1 条结构性错位（②）、1 条我踩过的比较口径错误（⑥）。

**这一步做在前面，代价是半小时；做在后面，代价是这一轮反复返工。**
